## Logic Pro
Due to the way Logic Pro sandboxes plugins, you may need to set your Connector instances to **LOCAL HOST** mode instead of **APP** for sound to be transmitted between sender and receiver nodes.[^1]

[^1]: Discovered by apan of Gearspace.